import React from 'react';
import ' ./App.scss';
const done = (fdsf) => {
	return true;
};
const App = (props) => {
	return (
		<div className={`${styeles.app} ${sfsdf}`} onClick={console.log('di')}>
			<span>{hi}</span>
		</div>
	);
};

export default App;
